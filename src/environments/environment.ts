// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyAa6_YC3bisq_jqveghB88LHZJi1dW_7SY',
    authDomain: 'f-transport.firebaseapp.com',
    databaseURL: 'https://f-transport.firebaseio.com',
    projectId: 'f-transport',
    storageBucket: 'f-transport.appspot.com',
    messagingSenderId: '215640729795',
    appId: '1:215640729795:web:c08a493c0de83853299a76'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
